# Model assets

This directory contains the licensed ONNX export of the open-source
`all-MiniLM-L6-v2` model as `onnx/model.onnx` plus its external tensor data and
tokenizer. The binary model is loaded locally and is never downloaded at runtime.
