# Contributing

1. Create a branch from `master`.
2. Install development dependencies with `pip install -e ".[dev]"`.
3. Run `pytest` and `ruff check .` before opening a pull request.
4. Keep public API changes documented in the README and changelog.

